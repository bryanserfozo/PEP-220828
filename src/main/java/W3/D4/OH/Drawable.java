package W3.D4.OH;

public interface Drawable {

    // This is an interface, interfaces work like contracts, essentially we are going to guarantee that whatever
    // class IMPLEMENTS (aka provides functionality) this interface, it will have these methods

    void draw(); // PUBLIC and ABSTRACT
}
